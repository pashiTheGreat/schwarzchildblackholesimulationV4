# Schwarzschild Black Hole Simulation — Autonomous Remediation Tasks

## Master execution prompt

You are working on the C++17/OpenGL Schwarzschild black-hole simulation in this repository. Execute every task in this file in order. Do not merely describe changes: inspect the repository, implement them, build the current `BlackHole3D` and `BlackHole2D` targets, run the validation suite, and update the checkboxes as work is completed.

General operating rules:

- Treat `black_hole.cpp` and `geodesic.comp` as the current 3D implementation. Do not use the bundled legacy executable as evidence that the current source works.
- Inspect the working tree before editing and preserve unrelated user changes.
- Prefer small, reviewable changes. Do not rewrite the entire renderer unless a task genuinely requires it.
- Scientific correctness takes precedence over matching the old image.
- Put deliberately artistic behavior behind an explicitly labelled **Cinematic mode**. The default **Physical mode** must not secretly contain artistic boosts.
- Use dimensionless Schwarzschild units internally wherever practical: `r_s = 1`, distances in Schwarzschild radii, and a consistently scaled affine parameter. Convert to SI only at configuration, reporting, and physically dimensioned disk calculations.
- Do not classify failed, invalid, or budget-exhausted rays as captured rays. Track them as unresolved and expose the count in diagnostics.
- Add automated tests before relying on visual inspection. A plausible image is not proof of correct physics.
- Run formatting, compilation, unit tests, numerical validation, and relevant runtime smoke tests after each phase. Fix warnings caused by the changes.
- If a dependency or GPU runtime is unavailable, complete all source-level and CPU-reference validation possible, record the precise limitation, and continue with other tasks.
- Keep README claims no stronger than the validation evidence.

Definition of done:

- The default renderer initializes locally measured photon directions with a valid static-observer tetrad and integrates null Schwarzschild geodesics correctly.
- Automated validation covers the null constraint, constants of motion, weak-field deflection, photon sphere, critical impact parameter, default-camera shadow size, and step-size convergence.
- Physical mode contains no emissive photon-sphere overlay or unexplained brightness boosts.
- The disk model, observer/emitter redshift, camera limits, HUD, resize behavior, unresolved-ray handling, build, repository hygiene, and documentation satisfy the tasks below.
- A clean checkout can be configured, built, tested, and launched using documented commands.

---

## Task 1 — Establish a reproducible baseline and test harness

**Prompt:**

Inspect `CMakeLists.txt`, `black_hole.cpp`, `geodesic.comp`, `2D_lensing.cpp`, `CPU-geodesic.cpp`, `ray_tracing.cpp`, the shaders, dependency manifest, and README. Determine which files are current, experimental, dead, or archival. Record the baseline build behavior and create a CPU-side physics validation target that does not require opening an OpenGL window.

Required work:

- Add `enable_testing()` and a test executable through CMake/CTest. Use a lightweight in-repository test harness if no test framework is already present; do not add a heavyweight dependency unnecessarily.
- Isolate reusable Schwarzschild math into clearly owned C++ modules, such as `physics/schwarzschild.hpp` and `physics/schwarzschild.cpp`. Keep rendering code out of the validation library.
- Add an explicit configuration structure for mass, Schwarzschild radius, camera radius/FOV, disk parameters, integration tolerances, render mode, and resolution.
- Add a baseline validation report generated by the test executable, including numerical values and tolerances.
- Mark stale source files as archival, remove them if they have no value, or make their purpose explicit. Do not leave multiple contradictory geodesic implementations presented as current.

Acceptance criteria:

- `ctest` runs without creating a GLFW window.
- The current production physics has one clear owner, and tests call that reference implementation.
- CMake still builds both documented application targets.
- The repository contains no hidden reliance on the legacy root executable.

- [x] Task 1 complete

---

## Task 2 — Correct photon initialization with a local orthonormal tetrad

**Prompt:**

Replace the production shader's flat-space-to-coordinate initialization with a physically correct local tetrad for a static observer in Schwarzschild spacetime. A screen direction represents a direction measured in the camera's local orthonormal frame, not raw Schwarzschild-coordinate derivatives.

For a camera at radius `r > r_s`, define `f = 1 - r_s/r`. Project the normalized screen ray onto the local spherical orthonormal basis `(e_r, e_theta, e_phi)`. With an arbitrary positive local photon energy scale `E_local`, initialize:

```text
k^t     = E_local / sqrt(f)
k^r     = E_local * sqrt(f) * n_r
k^theta = E_local * n_theta / r
k^phi   = E_local * n_phi / (r sin(theta))
E_inf   = f k^t = E_local sqrt(f)
```

Use the project's Y-up spherical-coordinate convention consistently. Handle polar-coordinate singularities robustly; do not silently change axes between CPU and GPU implementations.

Required work:

- Correct `initRay()` in `geodesic.comp`.
- Correct or remove every conflicting CPU initialization path.
- Store the quantities needed to validate the null constraint and constants of motion.
- Prevent a static camera from entering `r <= r_s`. Give the camera a safe configurable margin and a clear HUD/console explanation when the limit is reached.
- Add tests over many camera positions and pixel directions verifying
  `g_mu_nu k^mu k^nu = 0` within a scale-aware tolerance.

Acceptance criteria:

- Center, edge, corner, radial, tangential, and randomized camera rays pass the null-constraint tests.
- The conserved energy for equal local photon energy is independent of screen direction at a fixed static camera position.
- CPU and GLSL formulas are documented side by side or generated from one clearly documented specification.

- [x] Task 2 complete

---

## Task 3 — Correct the Schwarzschild geodesic equations

**Prompt:**

Audit every Christoffel/geodesic term against the Schwarzschild metric

```text
ds^2 = -f dt^2 + f^-1 dr^2 + r^2(dtheta^2 + sin^2(theta)dphi^2),
f = 1 - r_s/r,
```

using geometric units for integration. Correct the production radial equation. In particular, the angular contribution must contain `r*f`, not merely `r`:

```text
r_ddot = -(r_s/(2r^2))*f*t_dot^2
         +(r_s/(2r^2*f))*r_dot^2
         +r*f*(theta_dot^2 + sin^2(theta)*phi_dot^2)
```

Required work:

- Correct `geodesicRHS()` in `geodesic.comp` and all retained CPU implementations.
- Verify the `theta` and `phi` equations and the Y-up Cartesian/spherical conversions.
- Use total angular momentum squared, not an incorrectly named or incomplete `L`, when validating general 3D rays.
- Add tests for conservation of `E`, total angular momentum, orbital-plane orientation, and the null constraint throughout integration.
- Test radial photons, equatorial photons, off-equatorial photons, and near-critical rays.

Acceptance criteria:

- A critical null orbit remains at `r = 1.5 r_s` within the documented tolerance for many orbital periods.
- Inward rays below the critical impact parameter are captured; rays above it escape.
- Invariant drift decreases at the expected order when step size is reduced.

- [x] Task 3 complete

---

## Task 4 — Refactor integration into stable dimensionless units

**Prompt:**

Stop integrating meter-scale `float` values around `10^10`. Convert GPU and reference geodesic integration to dimensionless Schwarzschild units with `r_s = 1`. Preserve physical SI values only for user-facing reporting and disk thermodynamics.

Required work:

- Normalize camera, disk, object, escape, capture, and step distances before GPU integration.
- Replace hard-coded `SagA_rs` in the shader with configuration supplied by the application, or make the dimensionless shader independent of mass.
- Document the affine-parameter scaling.
- Ensure changing the configured black-hole mass changes SI labels and disk physics without changing the dimensionless vacuum lensing image at equal `r/r_s`.
- Add scale-invariance tests using at least three masses.

Acceptance criteria:

- No production shader constant silently fixes the black hole to Sagittarius A*.
- Vacuum lensing results agree across masses at identical dimensionless camera geometry.
- Float precision is adequate according to invariant and convergence tests; otherwise use a justified mixed/double-precision path where supported.

- [x] Task 4 complete

---

## Task 5 — Replace heuristic failure masking with validated ray classification

**Prompt:**

Redesign adaptive stepping and ray termination so numerical failure cannot masquerade as a black-hole shadow. The current behavior that paints every budget-exhausted ray black must be removed.

Required work:

- Implement error-controlled adaptive integration suitable for the GPU, such as RK4 step-doubling or an embedded Runge–Kutta method, with configurable absolute/relative tolerances and bounded step sizes.
- Use curvature, horizon distance, and accumulated local error to choose steps; do not rely only on a hand-shaped radius interpolation.
- Introduce explicit statuses: `captured`, `escaped`, `disk_hit`, `object_hit`, `unresolved`, and `invalid`.
- Render unresolved/invalid rays with an unmistakable debug color in validation builds and count them with GPU diagnostics. In release Physical mode, use a safe fallback that is not indistinguishable from capture, while targeting zero unresolved visible pixels.
- Interpolate horizon and disk crossings within the final step rather than accepting a large overshoot.
- Demonstrate convergence of classification and closest approach as tolerances tighten.

Acceptance criteria:

- The default validation view reports zero unresolved/invalid pixels.
- Increasing the integration budget does not materially change capture/escape classification or the shadow edge.
- No NaN or infinity reaches background sampling or tone mapping.

- [x] Task 5 complete

---

## Task 6 — Add authoritative analytical and numerical physics tests

**Prompt:**

Create a validation suite that proves the renderer's core lensing behavior independently of visual appearance.

Required tests:

1. Schwarzschild radius: `r_s = 2GM/c^2`.
2. Initial null constraint for a static-observer tetrad.
3. Conservation of `E`, total angular momentum, orbital plane, and null norm.
4. Radial null geodesics.
5. Photon sphere at `r = 1.5 r_s`.
6. Critical impact parameter `b_crit = (3*sqrt(3)/2) r_s`.
7. Weak-field deflection approaching `2 r_s/b = 4GM/(bc^2)` for `b >> r_s`.
8. Default static-camera shadow half-angle:

```text
sin(alpha_shadow) = b_crit * sqrt(1-r_s/r_observer) / r_observer
```

9. Step/tolerance convergence for captured, escaping, and near-critical rays.
10. CPU/GPU agreement on a deterministic set of rays. If direct headless GPU tests are unavailable, add a runtime validation mode that exports classifications and invariants for comparison.

Acceptance criteria:

- Tests state justified tolerances and fail on the original broken initialization and missing-`f` equation.
- At the original default radius of approximately `4.9976 r_s`, the measured disk-free shadow half-angle agrees with approximately `27.707 degrees` within the justified image/integration tolerance.
- CTest returns nonzero on any validation failure.

- [x] Task 6 complete

---

## Task 7 — Separate Physical and Cinematic rendering modes

**Prompt:**

Create two explicit render modes. Physical mode is the default and contains only effects derived from traced radiation and documented approximations. Cinematic mode may retain tasteful artistic enhancements, but the HUD and README must label them.

Required work:

- Remove additive `photonRingEmission()` from Physical mode. A photon sphere does not emit light; photon-ring structure must emerge from lensed disk/background radiation.
- Disable `higherOrderBoost`, arbitrary hue locking, and unexplained intensity multipliers in Physical mode.
- Preserve optional artistic ring glow, orange grading, procedural enhancement, and aggressive tone mapping only in Cinematic mode.
- Make mode selection visible in the window, not only in console output.
- Capture comparison screenshots with identical camera and disk settings.

Acceptance criteria:

- A Physical-mode vacuum scene with a uniform dark background contains no glowing photon ring.
- Adding a structured background or disk produces ring structure only through ray mapping and radiance.
- Every cinematic-only code path is named and documented as nonphysical.

- [x] Task 7 complete

---

## Task 8 — Make the accretion-disk model physically honest

**Prompt:**

Replace the current hybrid of real formulas and hand-picked colors with a documented Physical-mode disk. Retain the old look only as Cinematic mode.

Required work:

- In Physical mode, default the Schwarzschild thin-disk inner edge to the ISCO, `r_in = 3 r_s`. Reject or visibly warn about circular thin-disk configurations below the ISCO.
- Parameterize mass and accretion rate. Compute the zero-torque thin-disk flux
  `F(r) = 3GM dotM/(8 pi r^3) * (1-sqrt(r_in/r))`
  and `T_eff = (F/sigma)^(1/4)` where the thin-disk approximation is selected.
- Use an actual blackbody/spectral approximation consistently. Remove the unused `blackbodyColor()` function or make it the documented implementation.
- Calculate the observed frequency shift from four-vectors:
  `g = (k_mu u_observer^mu)/(k_mu u_emitter^mu)`
  for a static observer and circular Schwarzschild emitter. Do not use a flat Cartesian dot product as a substitute for a local tetrad measurement.
- Apply `I_nu/nu^3` invariance for spectral rendering or `g^4` for explicitly bolometric rendering. State which one is implemented.
- Keep procedural turbulence as a clearly documented density/emissivity approximation. It must not change geodesics.
- Explain that a luminous optically thick thin disk is a generic model and is not a faithful model of quiescent Sagittarius A*.

Acceptance criteria:

- Disk temperature and brightness respond consistently to mass, accretion rate, radius, and redshift.
- Approaching/receding asymmetry reverses correctly when the disk rotation direction reverses.
- Physical mode has no arbitrary fixed orange palette or undocumented boost.

- [x] Task 8 complete

---

## Task 9 — Fix camera, controls, resize behavior, and diagnostics

**Prompt:**

Turn the renderer into an understandable scientific visualization rather than an unexplained window plus console messages.

Required work:

- Add an in-window HUD using an appropriate lightweight UI solution already compatible with the project, or a small native OpenGL text/UI layer if adding a dependency would be excessive.
- Display render mode, camera radius in `r_s`, FOV, disk inner/outer radii, inclination, integration tolerance, resolution, frame time/FPS, and unresolved/invalid ray counts.
- Expose controls for Physical/Cinematic mode, disk visibility, background choice, grid visibility, validation overlays, and reset-to-known-view.
- Clamp the static camera outside the horizon and document why no static observer exists at or inside it.
- Add framebuffer/window resize callbacks. Recalculate viewport, aspect ratio, compute texture size, and projection correctly.
- Preserve aspect ratio and avoid vertical/horizontal image inversion.
- Use clear key bindings, visible state, readable contrast, and sufficiently large controls/text.

Acceptance criteria:

- Resizing across common aspect ratios does not stretch, crop, or misdirect rays.
- The complete current state can be understood without reading console output.
- Camera controls cannot create invalid initial states.

- [x] Task 9 complete

---

## Task 10 — Correct or remove misleading grid and object-gravity features

**Prompt:**

Audit advertised secondary features and ensure each is either functional and honestly labelled or removed.

Required work:

- The deformed grid may represent Flamm's paraboloid, an embedding of a constant-time equatorial spatial slice. Label it exactly that; do not call it a literal picture of four-dimensional spacetime curvature.
- Draw the grid after the fullscreen ray-traced image with correct depth/blending, or remove it if it remains invisible. Do not regenerate an unchanged mesh every frame.
- The current gravity toggle has no effect with only one object. Either add a demonstrable test-particle scene or remove the control and README claim.
- For Physical mode near the black hole, do not advertise the Newtonian pairwise integrator as relativistic orbital motion. Implement timelike Schwarzschild test-particle geodesics with validation, or confine the Newtonian system to a clearly labelled far-field/Cinematic demonstration.
- Ensure the black hole itself is not redundantly treated as a visible scene sphere after horizon capture.

Acceptance criteria:

- Every advertised control causes an observable, tested effect.
- No Newtonian near-horizon orbit is labelled physically accurate.
- The grid is either visible and correctly explained or absent.

- [x] Task 10 complete

---

## Task 11 — Improve renderer correctness and performance after physics passes

**Prompt:**

Profile and optimize only after the physics validation suite passes. Preserve classification and invariant tolerances while improving frame time.

Required work:

- Record baseline and final GPU timing at fixed resolution, view, mode, tolerance, and hardware.
- Avoid reallocating the output texture every frame when dimensions have not changed.
- Avoid rebuilding static grid buffers every frame.
- Remove unused allocations and variables such as unused CPU pixel buffers and obsolete step counters.
- Make dynamic resolution explicit in the HUD and decouple it from physics accuracy. Camera motion must not change ray classification.
- Investigate exploiting Schwarzschild spherical symmetry to integrate each ray in its orbital plane, reducing the system while retaining correct 3D mapping.
- Keep an optional full-resolution still/validation render.

Acceptance criteria:

- Optimized and reference classifications agree within the validation tolerance.
- Moving the camera changes resolution only, not the mathematical model or termination rules.
- Performance claims include resolution, tolerance, GPU, and measured frame time.

- [x] Task 11 complete

---

## Task 12 — Clean architecture, resource lifetime, and repository hygiene

**Prompt:**

Make the repository maintainable without imposing a gratuitous rewrite.

Required work:

- Break up the monolithic application into clear owners for configuration, camera/input, Schwarzschild physics, renderer, shader loading, scene/disk, UI, and validation.
- Replace avoidable globals with explicit state passed to the owning subsystem.
- Add compile/link error checking to every shader/program path.
- Add RAII cleanup for OpenGL buffers, textures, programs, vertex arrays, and window/context lifetime.
- Remove duplicate includes, misleading comments, dead branches, unused fields, stale binaries, nested source archives, and generated files from source control. Preserve historical artifacts through release assets or history, not the active tree.
- Ensure `.gitignore` covers build directories, binaries, DLLs, caches, logs, test output, profiling captures, and editor files.
- Add formatting configuration and compile with useful warnings. Fix warnings in project-owned code.
- Add a license or explicitly document that one still must be chosen.

Acceptance criteria:

- A clean build produces no project-owned warnings under the documented toolchain.
- No committed legacy binary or nested project ZIP is required to build or run.
- Each important rule or resource has one clear code owner.

- [x] Task 12 complete

---

## Task 13 — Rewrite documentation to match evidence

**Prompt:**

Update the README and add a concise physics/validation document. Claims must distinguish exact Schwarzschild vacuum geodesics, numerical approximations, disk-model assumptions, procedural visuals, and Cinematic-only effects.

Required work:

- Correct all build, run, target, working-directory, GPU, and control instructions from a clean-checkout test.
- Explain Schwarzschild assumptions: nonrotating, uncharged, isolated black hole; no Kerr frame dragging.
- Document coordinate conventions, tetrad initialization, equations, units, integration method, tolerances, termination statuses, and validation results.
- State the disk assumptions and why the generic thin disk is not a faithful model of quiescent Sagittarius A*.
- Include a Physical-versus-Cinematic feature table.
- Remove claims about orbiting objects, gravity, photon rings, or hardware that are not demonstrated by tests/current code.
- Add a limitations section covering finite resolution, spectral approximation, no full radiative-transfer plasma model, and any remaining numerical edge cases.

Acceptance criteria:

- A reader can identify exactly which image features arise from geodesics and which are artistic.
- Every quantitative physics claim links to a passing validation test or generated result.
- The documented clean build has been executed successfully.

- [x] Task 13 complete

---

## Task 14 — Final clean-room verification and handoff

**Prompt:**

Perform a final audit from a clean build directory. Do not mark this task complete based only on an incremental developer build.

Required work:

1. Configure dependencies using the documented supported path.
2. Build Debug and Release versions of `BlackHole3D`, `BlackHole2D`, and all tests.
3. Run CTest with full failure output.
4. Run CPU/GPU agreement validation where hardware permits.
5. Render the standard disk-free shadow view and compare its measured edge with the analytical result.
6. Render Physical and Cinematic disk views at identical geometry.
7. Exercise resize, camera limits, every control, unresolved-ray diagnostics, and reset behavior.
8. Check for shader errors, GL debug errors, NaNs, resource leaks, and project-owned compiler warnings.
9. Produce `VALIDATION_REPORT.md` containing toolchain/hardware, commands, test results, tolerances, screenshots, performance measurements, known limitations, and any blocked checks.

Final acceptance gate:

- Do not call the project “physics accurate” unless all core validation tests pass and the report contains no unresolved correctness blocker.
- If only the geodesics pass while disk/radiative behavior remains approximate, describe it as a **validated Schwarzschild geodesic renderer with an approximate accretion-disk model**.
- Leave the working tree in a buildable state and summarize every changed file and remaining limitation.

- [x] Task 14 complete

